# Tick source: 'binance' or 'csv'
# Default is 'binance' if not set
TICK_SOURCE=binance

# CSV file path (only used when TICK_SOURCE=csv)
CSV_FILE=./data/sample.csv

# Save ticks to CSV (for recording)
SAVE_TICKS=false

# Server port
PORT=3000
